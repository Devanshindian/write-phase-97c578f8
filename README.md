# The Write Phase

How a research bundle becomes a finished, publish-ready article.

**Read the walkthrough:** https://devanshindian.github.io/write-phase-97c578f8/

This repo holds the reference page plus the exact prompt files the engine runs:

- `prompts/` — the 33 AI prompts, one file per call, plus the shared style ruleset
- `formats/` — the 8 archetype playbooks that set each article shape
